// let collection =["shashi",23,true,{name: "madhu"}]
// pop(): ("Removes the element from an array and returns that element")

let books = ["maths", "physics", "social"];

let someResult = books.pop();

document.write(books + "<br>");

document.write("Deleted by pop method is " + someResult);
